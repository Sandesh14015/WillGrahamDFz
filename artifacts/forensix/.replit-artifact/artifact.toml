kind = "web"
previewPath = "/"
title = "ForensiX"
version = "1.0.0"
id = "artifacts/forensix"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21928

[services.development]
run = "pnpm --filter @workspace/forensix run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/forensix", "run", "build" ]
publicDir = "artifacts/forensix/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21928"
BASE_PATH = "/"
